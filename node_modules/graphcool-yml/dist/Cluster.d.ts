import 'isomorphic-fetch';
export declare class Cluster {
    name: string;
    baseUrl: string;
    local: boolean;
    private clusterToken?;
    constructor(name: string, baseUrl: string, token?: string, local?: boolean);
    readonly token: string | undefined;
    getApiEndpoint(serviceName: string, stage: string): string;
    getWSEndpoint(serviceName: string, stage: string): string;
    getImportEndpoint(serviceName: string, stage: string): string;
    getExportEndpoint(serviceName: string, stage: string): string;
    getDeployEndpoint(): string;
    isOnline(): Promise<boolean>;
    getVersion(): Promise<string | null>;
}
